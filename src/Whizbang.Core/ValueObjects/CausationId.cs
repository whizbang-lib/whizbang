// CausationId is just a MessageId of the parent/causing message.
// No need for a separate type - use MessageId directly.

// Empty namespace declaration so Roslyn RCS1093 ("File contains no code") does not flag this
// placeholder file — the comment above is the intended documentation.
namespace Whizbang.Core.ValueObjects;
