Format all code in the solution using dotnet format.

Execute: `dotnet format`

**This is MANDATORY before every commit.**

Formats code according to .editorconfig rules:
- File-scoped namespaces
- 4-space indentation
- LF line endings
- Async naming conventions enforced
- Null reference type checks

Run this command ALWAYS before claiming work is complete.
